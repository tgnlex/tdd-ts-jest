{
  "name": "ts-jest-tdd-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "jest --runInBand --detectOpenHandles --forceExit --verbose --coverage --testTimeout=10000",
    "watch": "jest --watch",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },

  "dependencies": {
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.10",
    "@types/node": "^20.9.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "tslib": "^2.6.2",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
